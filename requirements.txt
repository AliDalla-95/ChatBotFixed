# Runtime requirements for the Telegram bots in this repository.
# (After removing screenshot/OCR processing pipeline)
#
# Main entrypoints typically used:
# - bot_user.py
# - support.py
# Other scripts in this repo may also be used (client.py, send_mony.py, client_paid.py, youtube_chanel.py).

# Telegram bots (ApplicationBuilder/ContextTypes)
python-telegram-bot>=20

# Database
psycopg2-binary

# Phone parsing (used by bot_user.py / client.py)
phonenumbers

# Google API client (used by client.py / youtube_chanel.py)
google-api-python-client
cachetools

# System/process utilities (used by client.py)
psutil
